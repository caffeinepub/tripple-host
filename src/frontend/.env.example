# Tripple Host - Environment Configuration Template
# Copy this file to .env and configure for your deployment target

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# Target network: 'local' for development, 'ic' for mainnet production
# Default: local
DFX_NETWORK=local

# =============================================================================
# INTERNET IDENTITY CONFIGURATION
# =============================================================================

# Internet Identity Provider URL
# 
# For LOCAL DEVELOPMENT (dfx start):
#   Use the local Internet Identity canister
#   II_URL=http://rdmx6-jaaaa-aaaaa-aaadq-cai.localhost:4943
#
# For IC MAINNET PRODUCTION:
#   Use the official Internet Identity service
#   II_URL=https://identity.ic0.app
#
# Default: http://rdmx6-jaaaa-aaaaa-aaadq-cai.localhost:4943
II_URL=http://rdmx6-jaaaa-aaaaa-aaadq-cai.localhost:4943

# =============================================================================
# CANISTER CONFIGURATION
# =============================================================================

# Note: Canister IDs are automatically loaded at runtime from env.json
# (generated during build). You do NOT need to set them here.
#
# The build process generates env.json with:
# - Backend canister ID
# - Network configuration
# - Internet Identity derivation origin
#
# This file is automatically created by 'dfx generate' and 'dfx deploy'

# =============================================================================
# DEPLOYMENT EXAMPLES
# =============================================================================

# Example 1: Local Development
# -----------------------------
# DFX_NETWORK=local
# II_URL=http://rdmx6-jaaaa-aaaaa-aaadq-cai.localhost:4943

# Example 2: IC Mainnet Production
# ---------------------------------
# DFX_NETWORK=ic
# II_URL=https://identity.ic0.app

# =============================================================================
# ADDITIONAL NOTES
# =============================================================================

# 1. After changing DFX_NETWORK, rebuild the frontend:
#    pnpm run build:skip-bindings
#
# 2. The frontend automatically detects the network and loads the correct
#    canister IDs from env.json at runtime
#
# 3. For production deployment, ensure II_URL points to the official
#    Internet Identity service (https://identity.ic0.app)
#
# 4. Never commit .env to version control (it's in .gitignore)
